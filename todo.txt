Cursor or Codex todo

why cant i upload  photo videos or files in posts or chat or comments on production build (it says failed to fetch or load fail) every other features work. also why is there no auto thumbnails on mobile safari or chrome

unread count is incorrect, we should use read POST /v1/api/messages/{id}/read to mark messages as read so both navbar and chat unread msg counts use the same value that come from the backend not just optimistic ui fix

